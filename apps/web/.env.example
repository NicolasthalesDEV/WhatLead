# ===========================================
# Variáveis de Ambiente - WhatLead Hotel CRM
# ===========================================
# Copie este arquivo para .env.local e preencha os valores

# -----------------------------------------
# Autenticação (OBRIGATÓRIO)
# -----------------------------------------
# Chave secreta para JWT - mínimo 16 caracteres
JWT_SECRET=sua-chave-secreta-aqui-minimo-16-chars

# -----------------------------------------
# Banco de Dados (OBRIGATÓRIO)
# -----------------------------------------
# URL de conexão com pooling (para serverless)
DATABASE_URL=postgresql://usuario:senha@host:5432/database?sslmode=require&pgbouncer=true

# URL de conexão direta (para migrations)
DIRECT_URL=postgresql://usuario:senha@host:5432/database?sslmode=require

# -----------------------------------------
# Redis/BullMQ (OPCIONAL para Vercel)
# -----------------------------------------
# Se não configurado, funcionalidades de filas serão desabilitadas
REDIS_URL=redis://usuario:senha@host:6379

# Prefixo para filas BullMQ
BULLMQ_PREFIX=wacrm

# -----------------------------------------
# WhatsApp Integration
# -----------------------------------------
# Token de verificação do webhook
WA_VERIFY_TOKEN=seu-token-de-verificacao

# -----------------------------------------
# Provedor de Pagamentos
# -----------------------------------------
# Opções: fake, mercadopago, stripe
PSP_PROVIDER=fake

# -----------------------------------------
# Configurações da Aplicação
# -----------------------------------------
# URL pública da aplicação (para OAuth callbacks, etc)
NEXT_PUBLIC_APP_URL=https://seu-dominio.vercel.app

# Ambiente
NODE_ENV=production

# -----------------------------------------
# Build (não alterar)
# -----------------------------------------
# Usado pelo Vercel para skip da validação durante build
# SKIP_ENV_VALIDATION=true
